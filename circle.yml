machine:
    node:
        version: 4.1
    environment:
        LD_LIBRARY_PATH: /usr/local/lib
dependencies:
    pre:
        - sudo apt-get update -q; sudo apt-get install automake-1.14 g++-5.0
test:
    override:
        - bash run_tests.bash
